apiVersion: v1
kind: Service
metadata:
  name: {{ include "mern-auth.fullname" . }}
  labels:
    {{- include "mern-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mern-auth.selectorLabels" . | nindent 4 }}
